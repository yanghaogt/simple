<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syg.manage.dao.manage.SurveyRecordMpr">

	<select id="queryByscId" resultType="SurveyRecord">
		SELECT DISTINCT(du_id) FROM survey_record WHERE sc_id = #{scId};
	</select>
	
</mapper>